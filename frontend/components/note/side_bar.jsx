import React from 'react';
import BarNavContainer from './note/bar_nav_container';

const SideBar = (props) => {
  return (
    <div className='side-bar'>
      <BarNavContainer />
    </div>
  );
};

export default SideBar;
