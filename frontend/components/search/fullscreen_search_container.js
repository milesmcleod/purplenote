import { connect } from 'react-redux';
import FullscreenSearch from './fullscreen_search';

const mapStateToProps = (state) => {
  return {

  };
};

const mapDispatchToProps = (dispatch) => {
  return {

  };
};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(FullscreenSearch);
