import React from 'react';
import SideBarContainer from './note/bar_nav_container';

const Home = (props) => {
  return (
    <div className='home'>
      <SideBarContainer />
    </div>
  );
};

export default Home;
